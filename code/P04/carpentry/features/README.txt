In this paper I prepared code to characterize with a set of features the location of the
tick bites. However, we gradually moved to characterizing the pixel centroid in which the
tick bite (or many) occurred. This is why some file names have the suffix "_centroids".